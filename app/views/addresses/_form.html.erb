<%= form_for [@trip, @location, @address] do |f| %>
<%= t.label :street %>
<%= t.text_field :street %>

<%= t.label :state %>
<%= t.text_field :state %>

<%= t.label :zip %>
<%= t.text_field :zip %>

<%= f.submit %>
